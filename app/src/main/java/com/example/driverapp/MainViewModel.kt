package com.example.driverapp

import androidx.lifecycle.ViewModel
import com.example.driverapp.repository.TripRepository

class MainViewModel(private val repository: TripRepository) : ViewModel() {

}